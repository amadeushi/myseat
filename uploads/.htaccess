# Uploaded pictures only: never execute anything from here.
<FilesMatch "\.(php|phtml|php[0-9]|phar|pl|py|cgi)$">
	<IfModule mod_authz_core.c>
		Require all denied
	</IfModule>
	<IfModule !mod_authz_core.c>
		Order deny,allow
		Deny from all
	</IfModule>
</FilesMatch>
